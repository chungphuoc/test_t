- title = params[:statuses].present? ? 'Passed Class' : 'Booked Class'
- rating = params[:statuses].present? ? true : false
= render partial: "shares/course_booked", locals: { title: title, rating: rating }
